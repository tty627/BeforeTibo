# Repo Book

Consumes a clean committed source snapshot. One stage produces one independently reviewable guide unit with evidence.json and limitations. No repository command is executed. The runner checks scope, immutable source, required files and exact source references; the HTML report safely escapes content. Accepted means reference checks passed, not that all explanations were semantically proved.
