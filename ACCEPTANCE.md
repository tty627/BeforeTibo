# BeforeTibo 验收矩阵

状态初始全部为 NOT RUN。此表是待实现测试的合同，不是已完成测试结果。自动化测试应以对应 ID 命名或建立可追溯映射；真实环境测试可附脱敏证据。

## 工程与合同

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-01 | 当前目录包含无关项目或未提交文件 | 不覆盖、不 reset、不自动 stash；选择明确授权路径 |
| AC-02 | 工具不可用 | 明确指出具体缺失；不虚构安装或权限状态 |
| AC-03 | 干净目录安装构建 | npm ci、typecheck、build 按实际文档工作 |
| AC-04 | Node 不在支持线 | 可理解地拒绝或警告，不偶然崩溃 |
| AC-05 | 提供的 schema 与示例 | 所有正例通过，负例拒绝 |
| AC-06 | Recipe 使用未知 validator 或非法参数 | 派发前拒绝，零模型任务 |
| AC-07 | 重复 stage ID、缺阶段依赖或循环 | 语义校验拒绝 |
| AC-08 | Recipe 请求未授权权限或无限单元 | 拒绝；不扩大用户策略 |

## 持久化与 mock

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-09 | 非法状态转移 | reducer 拒绝；不改写事实 |
| AC-10 | 同一个 run 启动两个 runner | 仅一个 writer；另一个明确拒绝 |
| AC-11 | dispatch intent 落盘后进程启动前失败 | 名额仍计入；恢复不重复自动派发 |
| AC-12 | journal 最后一行截断 / 中段损坏 | 分别隔离尾部或拒绝恢复，准确报告 |
| AC-13 | 重放相同事件与晋升 | 不重复计数、不重置预算 |
| AC-14 | 无 Codex、无登录、离线 demo | demo 完整结束并生成可读示例报告 |
| AC-15 | DEMO 与真实模式 | 面板、JSON、receipt 均可区分；不静默切换 |
| AC-16 | mock 返回错误、延迟、重复事件 | 消费接口与真实 adapter 相同，错误进入正常状态机 |

## 工作区与进程边界

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-17 | run 前后用户源目录 | 文件、tracked 状态与原 Git 元数据不被改写 |
| AC-18 | tracked dirty、无 commit、submodule | 明确 blocked；不自动修改仓库以满足条件 |
| AC-19 | `.env`、密钥、认证文件、node_modules | 未进入模型快照或公开成果；规则有反例测试 |
| AC-20 | `../`、绝对路径、盘符、UNC、symlink | 不越过允许路径；不靠单纯字符串前缀判定 |
| AC-21 | 文件/总大小超限 | 有界停止并显示实际覆盖范围 |
| AC-22 | 沙箱不可用 | 不执行真实任务，不回退全权限 |
| AC-23 | harmless 外部写入探针 | 未授权写入被技术边界拒绝 |
| AC-24 | validator 或工具尝试外网 | 拒绝；错误被记录，不误标功能通过 |
| AC-25 | 超时、失控子进程、PID 复用 | 终止仅本 run 所属进程；不误杀其他会话 |

## 启动授权与适配器

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-26 | doctor / plan | 不启动推理、不私自联网安装依赖 |
| AC-27 | 非交互 run 无明确风险确认 | 拒绝；JSON 配置中的值不自动等于用户同意 |
| AC-28 | API key / 未知 provider / 身份不明 | 首版真实运行 blocked，不能 fallback |
| AC-29 | 用户要求零额外支出且无硬费用控制 | 返回 BLOCKED_NO_HARD_BILLING_GUARD |
| AC-30 | JSONL 分块、合并、尾行无换行 | 正确解析，不丢终态 |
| AC-31 | 超长行、损坏 JSON、stderr 噪声 | 有界记录并准确结束，不无限内存增长 |
| AC-32 | exit 0 但候选 schema 无效 | 不 accepted |
| AC-33 | usage 缺失/累计/缓存/重复 | 不补 0、不双计，标明覆盖范围 |

## 文档和成果存储

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-34 | Repo Book overview | 内容对应指定快照、有真实引用和限制 |
| AC-35 | 文档引用不存在路径、越界行号或错误哈希 | 引用验证失败；不默认展示为已验证 |
| AC-36 | 作者意图与未经执行的命令 | 标为推断或未执行，不虚构实验 |
| AC-37 | 模型尝试写最终 manifest / journal | 不获得验收权；越界被拒绝或隔离 |
| AC-38 | 同一单元产出多文件/版本 | 聚合为逻辑成果；不夸大独立成果数量 |
| AC-39 | 移除 Codex 后执行 harvest | 仅靠本地记录成功构建报告 |
| AC-40 | 晋升中崩溃 | 恢复同一 artifact；已验收文件不会被覆盖 |

## 调度与恢复

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-41 | 达到 max_dispatches | 首次尝试、修复、模型评审均占名额；不额外派发 |
| AC-42 | 达到墙钟/单任务期限 | 停新任务并有界终止，保留已完成成果 |
| AC-43 | 连续无进展 | 达到阈值后收尾，不制造重复内容维持运行 |
| AC-44 | 单元连续失败 | 修复次数上限生效，失败准确分类 |
| AC-45 | 第一次 Ctrl+C / 默认 stop | 不开新任务，有界 drain，不调用模型总结 |
| AC-46 | 二次 Ctrl+C / immediate | 尽快取消本 run 进程树，已有成果仍在 |
| AC-47 | intent 有记录但是否执行不明 | 恢复不自动重发，标 INTERRUPTED |
| AC-48 | 候选生成后退出 | 恢复先本地验收，不再次付费生成 |
| AC-49 | resume 原预算和原期限 | 不清零 dispatch，不把停机时间免费扣除 |
| AC-50 | 原期限已过 / 身份变化 | 只收获或明确阻止继续，不偷换账号 |

## 测试 Recipe

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-51 | 不支持的框架、monorepo、缺依赖 | 明确 blocked，不假装全语言支持 |
| AC-52 | 基线失败、未执行或发现 0 测试 | 不开始宣称新增保障 |
| AC-53 | 模型修改原测试、生产源码、配置、lockfile | 拒绝或隔离候选 |
| AC-54 | 新测试全 skipped / 无有效断言 / 未被发现 | 不以绿色退出码单独通过 |
| AC-55 | 稳定复现与未复现问题 | 依据和状态分开；不把所有失败称为 bug |

## CSV 工具

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-56 | 正常 added/removed/changed/unchanged | 与固定样例期望一致，JSON 导出正确 |
| AC-57 | BOM、引号、字段内逗号/换行、CRLF | 处理与规格一致，不静默丢行 |
| AC-58 | 重复/空主键、列差异、前导零、大小上限 | 明确拒绝或按字符串规范比较 |
| AC-59 | 恶意 HTML 文本、外网请求、缺浏览器验证 | 输入不执行；网络被阻止；缺验收不标功能通过 |

## 额度与账户

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-60 | observer 与 worker 账户不同或不可确认 | quota 模式拒绝，不错用余额 |
| AC-61 | 多桶、多窗口、未知窗口名称 | 使用实际元数据，不把比例相加、不硬编码周窗 |
| AC-62 | null、缺失、过期或非法百分比 | 显示 unavailable/stale，不当成 0 或无限 |
| AC-63 | observer 收到写请求或任务尝试兑换 reset | 白名单阻止，不购买、不兑换、不发邮件 |
| AC-64 | 不兼容的上游版本/字段 | 记录真实能力边界，禁用依赖功能 |
| AC-65 | 任一相关窗口达到保留阈值 | 停止新派发；风险升高时取消活动任务 |
| AC-66 | quota 快照变 stale | 不静默转 bounded；给出原因 |
| AC-67 | 额度比例上升或周期元数据变化 | 标可能变化并停新任务，不自动扩大预算 |
| AC-68 | 其他 Codex 会话同时消费 | 报告说明账户差值不等于本 run 独占消耗 |
| AC-69 | 服务限流 | 不换账号、不付费回退、不密集重试；期限仍生效 |

## 报告、错误、安全回归

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-70 | 非 TTY / NO_COLOR / --json | 不乱码；stdout 保持机器格式 |
| AC-71 | 部分成功 + 失败探索 | 正确分组，不把实验失败当成功工具 |
| AC-72 | 生成 HTML / Markdown / 链接 | 报告不执行不可信脚本、不加载外部跟踪资源 |
| AC-73 | 脱敏导出 | 无凭证、原始私有日志、绝对路径或未经同意的源码 |
| AC-74 | prompt injection 试图修改预算/验收 | 数据不能获得策略写权限，失败可观察 |
| AC-75 | 磁盘满、只读目录、写入失败 | 停派发，保留已验收成果，不虚报完成 |
| AC-76 | 配置、日志或路径含秘密 | 默认日志和分享产物不泄露，脱敏不能被单纯关闭 |
| AC-77 | 用户/机器时钟跳变 | 期限处理不无限延长；记录异常 |

## 打包、实测、开源

| ID | 场景 | 必须满足 |
|---|---|---|
| AC-78 | 从 npm pack 安装到临时目录 | CLI、schemas、recipes、模板均可定位 |
| AC-79 | 打包内容审查 | 不含 node_modules、凭证、真实 runs、私有日志 |
| AC-80 | README 安装命令 | 与实际源码/包发布状态一致，无占位命令假装可用 |
| AC-81 | fork PR 的 CI | 无个人 Codex 凭证、无真实模型消耗、最小权限 |
| AC-82 | GitHub Action 锁定 | 使用真实核实的完整 commit SHA，不是伪造值 |
| AC-83 | 支持声明和许可证 | 平台/框架范围真实；署名确认；用户产物许可不被擅自改写 |
| AC-84 | 真实 exec smoke | 有版本、结果和脱敏证据，或明确 NOT RUN |
| AC-85 | 三个 Recipe 真实联调 | 各自结果独立报告；不由单个通过推断其他通过 |
| AC-86 | 真实 quota 联调 | 明确桶映射、更新时间和身份一致；未测则禁用相应支持声明 |
| AC-87 | 发布前秘密/源码审查 | 扫描与人工检查涵盖已暂存文件和待推送历史 |
| AC-88 | 首次公开 owner/repo 或署名缺失 | 本地开发可完成；公开动作等待确认 |
| AC-89 | release readiness | 每项 PASS/FAIL/NOT RUN/BLOCKED 真实可追溯 |
| AC-90 | 同名仓库/已有 remote | 不覆盖、不替换、不 force-push |
| AC-91 | 创建/推送/Release | 分别核验真实结果；命令失败不宣称上传成功 |
| AC-92 | 未授权 npm / 发布用户成果 | 不执行；构建者权限不会进入 worker |

## 结果记录模板

每项记录 `ID / 测试路径或证据 / 环境 / 实际结果 / 运行时间 / 未通过原因`。使用 PASS、FAIL、NOT RUN、BLOCKED 四种结果；SKIPPED 不等于 PASS。涉及真实账户的证据须先脱敏，不能附原始认证材料。
